$id: https://schemas.ceeconnect.fr/enums/bar/type_chaufe_eau_solaire
$schema: https://json-schema.org/draft/2020-12/schema
title: Type de chauffe-eau solaire
type: string
oneOf:
  - $ref: "#/$defs/CESC"
  - $ref: "#/$defs/CESCI"
$defs:
  CESC:
    const: CESC
    title: Chauffe-eau solaire à appoint centralisé
  CESCI:
    const: CESCI
    title: Chauffe-eau solaire à appoint individualisé
  collectif:
    title: Type de chauffe-eau solaire
    type: string
    oneOf:
      - $ref: "#/$defs/CESC"
      - $ref: "#/$defs/CESCI"
