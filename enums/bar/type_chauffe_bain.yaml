$id: https://dictionnaire.ceeconnect.fr/schemas/enums/bar/type_chauffe_bain
$schema: https://json-schema.org/draft/2020-12/schema
title: Type de chauffe bain
type: string
oneOf:
  - $ref: "#/$defs/FLUX_FORCE_HAUT_RENDEMENT"
  - $ref: "#/$defs/COMBUSTION_HAUT_RENDEMENT"
  - $ref: "#/$defs/CONDENSATION"
$defs:
  FLUX_FORCE_HAUT_RENDEMENT:
    const: FLUX_FORCE_HAUT_RENDEMENT
    title: Chauffe bain à flux forcé à haut rendement
  COMBUSTION_HAUT_RENDEMENT:
    const: COMBUSTION_HAUT_RENDEMENT
    title: Chauffe bain à combustion atmosphérique à haut rendement
  CONDENSATION:
    const: CONDENSATION
    title: Chauffe bain à condensation
  maison:
    title: Type de chauffe bain
    type: string
    oneOf:
      - const: FLUX_FORCE_HAUT_RENDEMENT
        title: Chauffe bain à flux forcé à haut rendement
      - const: CONDENSATION
        title: Chauffe bain à condensation
  appartement:
    title: Type de chauffe bain
    type: string
    oneOf:
      - const: FLUX_FORCE_HAUT_RENDEMENT
        title: Chauffe bain à flux forcé à haut rendement
      - const: COMBUSTION_HAUT_RENDEMENT
        title: Chauffe bain à combustion atmosphérique à haut rendement
      - const: CONDENSATION
        title: Chauffe bain à condensation
