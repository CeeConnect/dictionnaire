$id: https://dictionnaire.ceeconnect.fr/schemas/enums/bar/profil_soutirage_chauffe_eau_solaire
$schema: https://json-schema.org/draft/2020-12/schema
$comment: "Chauffe-eau solaire indivicuel : réglement (UE) n°814/2013"
title: Profil de soutirage d'un chauffe-eau solaire
type: string
oneOf:
  - $ref: "#/$defs/M"
  - $ref: "#/$defs/L"
  - $ref: "#/$defs/XL"
  - $ref: "#/$defs/XXL"
$defs:
  M:
    const: M
    title: M
  L:
    const: L
    title: L
  XL:
    const: XL
    title: XL
  XXL:
    const: XXL
    title: XXL
  chauffeEauSolaireIndividuel:
    title: Profil de soutirage
    type: string
    oneOf:
      - $ref: "#/$defs/M"
      - $ref: "#/$defs/L"
      - $ref: "#/$defs/XL"
      - $ref: "#/$defs/XXL"
    examples:
      - M
