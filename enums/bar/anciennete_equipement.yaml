$id: https://schemas.ceeconnect.fr/enums/bar/anciennete_equipement
$schema: https://json-schema.org/draft/2020-12/schema
title: Ancienneté de l'équipement
type: string
oneOf:
  - $ref: "#/$defs/NEUF"
  - $ref: "#/$defs/EXISTANT"
$defs:
  NEUF:
    const: NEUF
    title: Équipement de moins de 2 ans
  EXISTANT:
    const: EXISTANT
    title: Équipement de plus de 2 ans
